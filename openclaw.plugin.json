{
  "id": "live-events",
  "name": "Live Events",
  "description": "Search live events near you via Ticketmaster Discovery API",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["apiKey"],
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Ticketmaster Discovery API key"
      },
      "defaultLocation": {
        "type": "string",
        "description": "Default city for event searches (e.g. \"Miami\")"
      }
    }
  },
  "uiHints": {
    "apiKey": { "label": "Ticketmaster API Key", "sensitive": true },
    "defaultLocation": { "label": "Default City", "placeholder": "Miami" }
  }
}
